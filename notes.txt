ios liquid glass rounded corner containers
bold striking cgi background
overall minimalistic clean vibe
3 pages: 
dashboard: overview of stock e.g total stock, uniqe games, instock, out of stock, low stock ect
games: shows all games by default with dropdown for categories
(can click on a game and it opens in  minimalistic popup)
drop down for sort by
about: simple page with some details about site and why it exists


todo:
remove img src and make it 1000games instead of 100,
make about page,
make games table scrollable instead of page (page scrolling with blur = bad preformance),
make sure inputs are sanitized,
add delete and edit functions (with password),
add some more cool animations,
fix the missalignment of the dropdowns



/* More matte / frosted */
backdrop-filter: blur(20px) saturate(80%);
background: rgba(255, 255, 255, 0.08);

/* Darker tinted glass */
background: rgba(0, 0, 0, 0.25);
backdrop-filter: blur(14px);

/* Colored tint (e.g. blue-ish) */
background: rgba(100, 149, 237, 0.15);
backdrop-filter: blur(12px);
